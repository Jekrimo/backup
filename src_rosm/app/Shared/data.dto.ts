import * as moment from 'moment';

export interface IUserLoginCreds {
  username: string;
  password: string;
}
export interface IPackageInfo {
  author: string;
  date: any;
  description: string;
  type: string;
  id: string;
}
